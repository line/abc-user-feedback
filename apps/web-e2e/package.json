{
  "name": "web-e2e",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.30.0",
    "typescript": "^4.9.5"
  },
  "scripts": {
    "pretest": "rm -rf && tsc --incremental -p tsconfig.json",
    "test": "playwright test -c build"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "typeorm": "^0.3.12"
  }
}
